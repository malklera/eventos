# Eventos

Commands:
- help
- version
- init -t --target [path] [name app]
- qr
- rename
- format
- edit 
- copy: use the rsync go library to copy the files
- catalogo: copy all screenshots from the cortado directory to catalogo, rename
them the same as the origin video

